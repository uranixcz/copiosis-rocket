<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width"/>
    <title>Copiosis</title>
    <style>
    table, th, td {
        border: 1px solid black;
        border-collapse: collapse;
        padding: 1px 10px;
    }

    </style>
</head>
<body>
<h1>Copiosis admin</h1>
<p>Neoficiální ukázka v raném vývoji</p>
<p>
    <a href="/users">Výpis uživatelů</a> &middot;
    <b><a href="/products">Výpis produktů</a></b> &middot;
    <a href="/transfers">Nejnovější transfery</a>
    <br>
    <a href="/adduser">Přidat uživatele</a> &middot;
    <a href="/product">Přidat produkt</a> &middot;
    <a href="/transfer">Transfer</a>
</p>
<table>
    <tr>
        <th>ID</th>
        <th>Jméno</th>
        {{#if ../is_user_product}}<th>Brána</th>{{/if}}
        {{#if ../is_user_product}}<th>Benefit</th>{{/if}}
        <th>Vytvořen</th>
        <th>Operace</th>
    </tr>
    {{#each vec}}
        <tr>
            <td>{{id}}</td>
            <td>{{#if ../is_user_product}}{{name}}{{else}}<a href="/product/{{id}}">{{name}}</a>{{/if}}</td>
            {{#if ../is_user_product}}<td style="overflow: hidden; max-width: 66px;">{{gateway}}</td>{{/if}}
            {{#if ../is_user_product}}<td style="overflow: hidden; max-width: 66px;">{{benefit}}</td>{{/if}}
            <td>{{time_created}}</td>
            <td>{{#if ../is_user_product}}Smazat (WIP){{else}}<a href="/product/{{id}}/producers">Producenti</a>{{/if}}</td>
        </tr>
    {{/each}}
</table>
</body>
</html>